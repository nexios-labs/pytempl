import { pytemplLanguage } from '../pytempl-lang.js'

export function pytemplHighlightPlugin() {
  return {
    name: 'pytempl-highlight',
    configureMarkdown(md) {
      // This is a placeholder - VitePress handles syntax highlighting differently
      // We'll use a different approach
    }
  }
}
